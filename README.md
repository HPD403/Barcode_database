# Barcode_database

## Running on Windows

A Windows launcher file is included in this repository.

1. Double-click `run_tool_inventory.bat`.
2. A terminal window will open and start the app.
3. The browser should open automatically to `http://localhost:8765`.

If the browser does not open automatically, open that address manually.
